"""Pure domain layer: entities, value objects, and domain services.

This package has zero dependencies on Qt, SQLAlchemy, HTTP clients, or any
other I/O concern. Everything here is plain Python (dataclasses, protocols,
and pure functions) so it can be unit tested without a database, a GUI, or
a network connection.
"""
